/* ---------------------------------------------------------------------- */

#define ns__command0 "::command"
static int
tcl__command0_actual(ClientData CD, Tcl_Interp *IP, int OC, Tcl_Obj *CONST OV[])
{
return TCL_OK;
}

static int
tcl__command0(ClientData CD, Tcl_Interp *IP, int OC, Tcl_Obj *CONST OV[])
{
  int _rv;
  TRACE_ARGS (ns__command0, OC, OV);
  _rv = tcl__command0_actual (CD, IP, OC, OV);
  TRACE_RESULT (IP, _rv);
}
