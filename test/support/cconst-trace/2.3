/* ---------------------------------------------------------------------- */

#define ns__the_alpha0 "::the::alpha"

static int
tcl__the_alpha0(ClientData cd, Tcl_Interp *interp, int oc, Tcl_Obj *CONST ov[])
{
  int rv;
  TRACE_ARGS (ns__the_alpha0, oc, ov);

  if (oc != 1) {
    Tcl_WrongNumArgs(interp, 1, ov, NULL);
    TRACE_RESULT (interp, TCL_ERROR);
  }

  /* Const - - -- --- ----- -------- */
  rv = 0;

  /* (bool return) - - -- --- ----- -------- */
	Tcl_SetObjResult(interp, Tcl_NewIntObj(rv));
	TRACE_RESULT (interp, TCL_OK);
}
