/* ---------------------------------------------------------------------- */

#define ns__the_alpha0 "::the::alpha"
static int
tcl__the_alpha0_actual(ClientData dummy, Tcl_Interp *ip, int objc, Tcl_Obj *CONST objv[])
{
static char script[4] = {
        98,101,116,97,
  };
  Tcl_SetByteArrayObj(Tcl_GetObjResult(ip), (unsigned char*) script, 4);
  return TCL_OK;
}

static int
tcl__the_alpha0(ClientData dummy, Tcl_Interp *ip, int objc, Tcl_Obj *CONST objv[])
{
  int _rv;
  TRACE_ARGS (ns__the_alpha0, objc, objv);
  _rv = tcl__the_alpha0_actual (dummy, ip, objc, objv);
  TRACE_RESULT (ip, _rv);
}
