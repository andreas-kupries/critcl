/* ---------------------------------------------------------------------- */

#define ns__aproc0 "::aproc"
static void c__aproc0(char* x)
{

}

static int
tcl__aproc0(ClientData cd, Tcl_Interp *interp, Tcl_Size oc, Tcl_Obj *CONST ov[])
{
  char* _x;

  if (oc != 2) {
    Tcl_WrongNumArgs(interp, 1, ov, "x");
    return TCL_ERROR;
  }

  /* (bytearray x) - - -- --- ----- -------- */
	{
	/* Raw binary string. Length information is _NOT_ propagated */
	_x = (char*) Tcl_GetByteArrayFromObj(ov[1], NULL); }

  /* Call - - -- --- ----- -------- */
  c__aproc0(_x);

  /* (void return) - - -- --- ----- -------- */
	return TCL_OK;
}
