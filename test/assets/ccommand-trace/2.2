/* ---------------------------------------------------------------------- */

static int
snafu_actual(ClientData clientdata, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[])
{
return TCL_OK;
}

static int
snafu(ClientData clientdata, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[])
{
  int _rv;
  TRACE_ARGS ("snafu", objc, objv);
  _rv = snafu_actual (clientdata, interp, objc, objv);
  TRACE_RESULT (interp, _rv);
}
