/* ---------------------------------------------------------------------- */

#define ns__aproc0 "::aproc"
static void c__aproc0(int anargument)
{

}

static int
tcl__aproc0(ClientData cd, Tcl_Interp *interp, int oc, Tcl_Obj *CONST ov[])
{
  int _anargument;

  if (oc != 2) {
    Tcl_WrongNumArgs(interp, 1, ov, "anargument");
    return TCL_ERROR;
  }

  /* (int anargument) - - -- --- ----- -------- */
	{
#line 4697 "critcl.tcl"
	if (Tcl_GetIntFromObj(interp, ov[1], &_anargument) != TCL_OK) return TCL_ERROR; }

  /* Call - - -- --- ----- -------- */
  c__aproc0(_anargument);

#line 4783 "critcl.tcl"
	return TCL_OK;
}
