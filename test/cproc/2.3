/* ---------------------------------------------------------------------- */

static void c_snafu()
{

}

static int
snafu(ClientData cd, Tcl_Interp *interp, int oc, Tcl_Obj *CONST ov[])
{

  if (oc != 1) {
    Tcl_WrongNumArgs(interp, 1, ov, NULL);
    return TCL_ERROR;
  }

  /* Call - - -- --- ----- -------- */
  c_snafu();

#line 4746 "critcl.tcl"
	return TCL_OK;
}
